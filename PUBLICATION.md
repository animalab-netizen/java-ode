# Publication Guide

`java-ode` is structured as a publishable JVM artifact.

Release checklist:

1. Compile main and test sources
2. Update `CHANGELOG.md`
3. Tag `v0.1.0`
4. Publish to Maven Central or another public JVM registry
