# UseCase Guide

Use `UseCase` for direct delivery, `ChainUseCase` for two-step dependent comparison, `SequenceUseCase` for ordered execution across three or more entries, and guards to stop invalid dispatch before execution begins.
